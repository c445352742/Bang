package main
import (
				// "net/http"
				"fmt"
)

func main() {
        // http.Handle("/", http.FileServer(http.Dir("http.Dir('/home/chai/src')))")))
				// http.ListenAndServe(":80", nil)
				res,err=cal()
				fmt.Println(a)
}
func cal() Error() String{
	return 0,Error() String
}
